# api-henry

Proyecto Java (Spring Boot) - api-henry

Quick start

```bash
# Build
mvn -B clean package

# Run
java -jar target/api-henry-1.0.0.jar
```

CI

Se incluye un workflow básico de GitHub Actions que compila y corre tests en Java 17 con Maven.
